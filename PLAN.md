# Oxc Migration Plan

## Current State

- Core `@wyw-in-js/transform` runtime/build path is on Oxc.
- Full local validation is green on branch `anber/oxc-migration-cutover`.
- The committed cutover snapshot is `0d7290ed` (`feat(transform): cut over core pipeline to oxc`).
- Remaining first-party Babel usage outside the core path is intentionally deferred for now:
  - `packages/babel-preset`
  - `linaria/packages/interop`
  - `linaria/packages/postcss-linaria`
- Validation blockers are currently cleared again; the remaining open problem is performance measurement/optimization plus the deferred first-party Babel cleanup outside the core path.

## Goals

1. Remove the Darkmatter perf regression without weakening behavior or test coverage.
2. Keep existing Oxc parity and validation green while optimizing.
3. Only after perf stabilizes, resume the remaining Babel cleanup outside the core transform path.

## Optimization Order

1. Add narrow perf instrumentation around the Oxc hot path so timing can be attributed to:
   - `applyOxcProcessors`
   - template dependency extraction
   - preeval transforms
   - shaker / prepare
   - emit
2. Land the cheapest low-risk wins first in `applyOxcProcessors`:
   - short-circuit when the module imports no known processors
   - reuse the first `collectProcessorUsages` result when extraction does not rewrite the code
3. Introduce shared exact `(filename, code)` parse caching across the Oxc helper stages that currently reparse the same module text.
4. Remove duplicate import/export reparsing in the processor path by reusing already-available AST/module-analysis results.
5. Re-run the perf baseline after each block and stop as soon as Darkmatter returns to parity or better.
6. If the cheap wins are not enough, move to the next tier:
   - collapse adjacent Oxc stages that currently do full extra walks over unchanged code
   - reduce repeated replacement sorting / full-string rewrite passes
   - consider lower-level Oxc-native transforms only for the hot spots proven by measurements

## Guardrails

- Do not rewrite behavior to chase perf.
- Do not delete old regression coverage to make optimization easier.
- Do not introduce a second parallel implementation path unless measurements prove it is necessary.
- Treat existing passing transform, adapter, and validation suites as the behavior contract.

## Findings

- `applyOxcProcessors` no longer reparses through each cleanup subpass; the remaining cost in that area is now the per-iteration analysis itself, not subpass-local reparsing.
- `oxcPreevalTransforms` still has whole-module duplication, but `import.meta.env` is separated from dynamic-import / require-fallback rewrites by the dangerous-code stage. A full single-pass collapse across all three would require reordering or offset rebasing, so the next low-risk slice is to merge the post-dangerous dynamic-import and require-fallback passes first.
- `collectOxcTemplateDependencies` now shares parse results, but still performs several whole-AST analysis passes (`bindings`, root mutations, used names, target expression discovery) that can be fused later if the new preeval sublabels point there.
- Inside `collectOxcTemplateDependencies`, the remaining cheap wins are now more about analysis reuse than parsing: repeated subtree `findReferences(...)` walks and per-recursion `ancestors` array copies still exist on the hot path.
- Fine-grained Oxc preeval timing is now available through the existing event-emitter path, so future perf work should follow measured hot buckets instead of broad guesses.
- A full `@wyw-in-js/transform test` run after the current perf work still surfaces older non-perf failures outside the touched files (`processImports` expectation drift and flaky temp-file concurrency cases). The preeval/processor suites touched by this optimization block are green; the unrelated package-test debt should be tracked separately from the hot-path work.
- Re-measurement after the latest preeval work shows the merged dynamic-import / require-fallback pass is effectively free in Darkmatter. The remaining preeval cost is dominated by `processTemplate`, then `removeDangerousCode`; `importMetaEnv` is materially smaller and no longer the best next target.
- `collectOxcTemplateDependencies` no longer copies `ancestors` arrays on every recursive step and now reuses cached subtree reference scans plus binding lookups during extraction. This is a low-risk runtime optimization layer over the existing semantics, not a logic rewrite.
- Snapshot `oxc-wip-20260424-r3` confirms the template-extraction caching pass helped without moving the bottleneck: baseline comparison is now `IMPROVEMENT`, Darkmatter is back in the stable band, and the hottest remaining preeval buckets are still `processTemplate`, then `removeDangerousCode`.
- `collectOxcTemplateDependencies` can absorb more of `processTemplate` by sharing one `Program` analysis between template discovery and extraction context setup. The low-risk version is to fuse `bindings`, `usedNames`, and target discovery while leaving root-mutation semantics untouched.
- The shared `Program`-analysis pass did surface a `build:types` regression during the first full `validate` run, but it was only a visitor-narrowing issue: moving template / target collection into a single pre-narrow helper preserved the optimization and restored the type gate without backing out the analysis fusion.
- `validate.sh --snapshot-release` on a dirty worktree measures the checked-out snapshot ref, not the current local changes. For perf work on uncommitted code, the reliable path is to snapshot a temporary tree commit (for example via `git stash create`) and compare that output instead of trusting a release snapshot built from `HEAD`.
- Corrected snapshot `oxc-wip-20260424-r5` shows the shared-analysis pass behaved as intended end to end: Portal improved further, Darkmatter build returned to a slight improvement/stable band, and only Darkmatter typecheck still warns. The remaining hot portal-side preeval buckets are `processTemplate`, then `removeDangerousCode`.
- Attempted fixed-point optimization inside `removeDangerousCode` did not survive end-to-end measurement: snapshot `oxc-wip-20260424-r6` regressed from `r5` (`Darkmatter build 16.387s` vs `15.260s`, total `18.185s` vs `17.192s`) even though focused preeval tests stayed green. That experiment is rejected and must stay reverted.
- The next low-risk hotspot inside `processTemplate` is no longer parsing itself but repeated lookup work during extraction: `resolveBindingAt` still allocates and sorts per reference, and insertion-point ownership still does a linear `program.body.find(...)` for every extracted expression.
- Snapshot `oxc-wip-20260424-r7` confirms the first lookup-overhead pass is safe: versus `r5`, Portal improved again (`53.422s`, `-1.17%`), Darkmatter typecheck improved (`1.778s`, `-7.97%`), and Darkmatter build/total stayed inside the stable band (`15.768s`, `+3.33%`; `17.546s`, `+2.06%`). That keeps the focus on `processTemplate`, but the next cheap target should move away from parse/lookup and into recursive evaluation overhead.
- The remaining low-risk extraction overhead now includes recursive stack cloning in static evaluation and hoist traversal (`[...stack, name]` plus repeated `includes()` checks). That is a better next candidate than revisiting `removeDangerousCode`, because it sits directly in the still-hot `processTemplate` bucket and does not require changing pass ordering.
- Snapshot `oxc-wip-20260424-r8` rejects that recursion-stack hypothesis: versus `r7`, Darkmatter regressed sharply (`build 17.708s`, `+12.30%`; `total 19.464s`, `+10.93%`), and versus the stable `r5` snapshot it became a `REGRESSION_FAIL` (`build +16.04%`). The focused suites stayed green, so the cost is performance-only, but that is enough to reject the change and keep the older stack behavior.
- After rejecting the stack rewrite, the next `processTemplate` target should be narrower than “static evaluation internals”. The remaining promising area is reference/scope traversal reuse (`findReferences`, `hasLocalBinding`, repeated subtree walks) rather than recursion bookkeeping.
- Snapshot `oxc-wip-20260424-r9` validates that narrower approach: caching scope-chain binding checks inside `findReferences` keeps Darkmatter in the safe band and improves it slightly versus both `r7` and `r5` (`total 17.057s`, `-2.79%` vs `r7`, `-0.79%` vs `r5`). Portal slowed relative to `r7` and `r5`, but stayed within the stable band (`55.721s`, `+4.30%` vs `r7`, `+3.08%` vs `r5`), so this slice is acceptable to keep while the next target shifts to Portal-heavy work.
- With `r9`, the remaining visible perf problem is no longer Darkmatter regression but Portal-side `processTemplate` cost. The next optimization should therefore target Portal-heavy subtree/reference duplication before revisiting any risky `removeDangerousCode` or recursion rewrites.
- `transform:preeval:processTemplate` is now instrumented internally through `applyOxcProcessors`, so the next profile can attribute time to imports, usage discovery, dependency extraction, reparsing, used-name collection, processor instantiation/callback, and cleanup instead of treating the whole processor path as one opaque bucket.
- Snapshot `oxc-wip-20260424-r10` makes that attribution actionable: versus `r5`, Portal regressed (`55.976s`, `+3.55%`) while Darkmatter improved (`14.769s` build, `-3.22%`; `16.598s` total, `-3.46%`). The useful result is the sub-breakdown itself: `processTemplate:imports` dominates both Darkmatter (`804ms` of `1624ms`) and Portal (`1894ms` of `3480ms`), while `processors`, `deps`, `reparse`, and `usedNames` are all materially smaller.
- The next bounded target should therefore move into import/processor discovery inside `applyOxcProcessors`, not back into template-dependency extraction. The safest first slice is to reduce repeated processor-resolution work and re-measure before attempting a new dedicated import-only analyzer.
- Snapshot `oxc-wip-20260424-r11` rejects that lookup-cache hypothesis as a meaningful end-to-end win. Portal returned to the good band (`54.033s`, essentially flat vs `r5`), but Darkmatter regressed (`16.233s` build, `+6.38%`; `18.004s` total, `+4.72%`), and the live preeval sublabels showed `processTemplate:imports` staying dominant in both Darkmatter (`1182ms`) and Portal (`1902ms`). Final processor-resolution caching is therefore acceptable to keep only as a tiny local improvement layer, not as the main remaining fix.
- With lookup reuse exhausted, the next justified move is structural but still local: give `applyOxcProcessors` an imports-only collector that keeps direct imports, CommonJS require declarators, and namespace/member resolution, while skipping the rest of the export/reexport analysis that the processor path never consumes.
- Snapshot `oxc-wip-20260424-r12` validates that imports-only move. Versus `r11`, Portal improved from `54.033s` to `51.990s` (`-3.78%`), Darkmatter build from `16.233s` to `14.450s` (`-10.99%`), and Darkmatter total from `18.004s` to `16.127s` (`-10.43%`). Versus the stable `r5` snapshot, the same block is still clearly positive (`Portal -3.82%`, `Darkmatter build -5.31%`, `Darkmatter total -6.19%`).
- The live `processTemplate:*` sublabels confirm the mechanism, not just the totals: Darkmatter `processTemplate:imports` dropped from `1182ms` in `r11` to `843ms`, and Portal from `1902ms` to `1586ms`. That makes the imports-only collector an accepted hotspot fix rather than noise.
- Snapshot `oxc-wip-20260424-r13` showed why the remaining imports bucket still mattered after `r12`: in Darkmatter, `processTemplate:imports` was no longer analyzer-heavy (`analysis 29ms`) but still mostly processor lookup (`lookup 751ms`). That ruled out deeper AST surgery as the next best move and pointed directly at the lookup/cache semantics instead.
- Snapshot `oxc-wip-20260424-r14` validates the broader package-backed lookup cache. Versus `r12`, Portal improved from `51.990s` to `47.139s` (`-9.33%`), Darkmatter build from `14.450s` to `13.577s` (`-6.04%`), and Darkmatter total from `16.127s` to `15.193s` (`-5.79%`). Versus the stable `r5` snapshot, the cumulative gain is now substantial (`Portal -12.80%`, `Darkmatter build -11.03%`, `Darkmatter total -11.63%`).
- The sublabels explain that win too: Darkmatter `processTemplate:imports` fell from `823ms` in `r13` to `335ms` in `r14` (`lookup 751ms -> 256ms`), and Portal from `1586ms` in `r12` to `730ms` in `r14` (`lookup 546ms`, `analysis 45ms`). Import discovery is no longer the dominant unresolved cost center it was before `r12`/`r14`.

## Progress

- Oxc cutover is complete for the core transform/evaluator/build path.
- Validation gate is green.
- Perf baseline has been captured and compared.
- Block 1 done: `applyOxcProcessors` now short-circuits the no-resolved-processor and no-usage paths, and reuses the first processor-usage scan when template extraction leaves code unchanged. Added regression coverage for the no-processor path; targeted `applyOxcProcessors` tests are green.
- Block 2 done: processor import discovery no longer reparses source text through `collectOxcExportsAndImports`; the analyzer now has a parsed-program entrypoint and `applyOxcProcessors` reuses its existing AST. Targeted `applyOxcProcessors` and `collectOxcExportsAndImports` tests are green.
- Block 3 done: added a shared capped Oxc parse cache for exact `(filename, code, sourceType)` reuse and connected it to the processor/template-dependency/import-analysis path. Targeted `applyOxcProcessors`, `collectOxcExportsAndImports`, and `collectOxcTemplateDependencies` tests are green.
- Block 4 done: re-measured the optimized path against `recalc-20260418-v2-esm-evaluator-snap-r1` using snapshot `oxc-wip-20260424-r1`. Result: `Portal build 53.669s` (`-9.72%`), `Darkmatter build 15.462s` (`+0.29%`, stable), `Darkmatter total 17.186s` (`-0.08%`, stable). The previous Darkmatter regression is no longer reproduced by the baseline comparator.
- Block 5 done: restored fine-grained Oxc preeval timing sublabels on the existing `EventEmitter` path (`processTemplate`, `importMetaEnv`, `removeDangerousCode`, `dynamicImport`, `requireFallback`) and covered them with focused tests. Future profiling can now target the hottest preeval substep instead of only the aggregate `transform:preeval` bucket.
- Block 6 done: collapsed the parse-heavy cleanup loop in `applyOxcProcessors` so each fixed-point iteration reuses one parsed program plus shared statement/reference/binding analysis, then applies merged cleanup removals in one shot. Targeted `applyOxcProcessors`, `prepareCode`, and `oxc-preeval-stage` tests remain green.
- Block 7 done: merged the post-dangerous dynamic-import and require-fallback rewrites in `oxcPreevalTransforms` into one parsed-program scan plus one replacement application, while preserving the existing perf sublabels and keeping standalone transform tests green. Targeted `oxc-preeval-transforms`, `oxc-preeval-stage`, and `prepareCode` suites are green.
- Block 8 done: re-measured against `recalc-20260418-v2-esm-evaluator-snap-r1` using snapshot `oxc-wip-20260424-r2`. Result: `Portal build 54.750s` (`-7.90%`), `Darkmatter build 16.019s` (`+3.90%`, stable), `Darkmatter total 18.017s` (`+4.75%`, stable but noisier than `r1`). Updated preeval buckets confirm `processTemplate` is still the main target; merged dynamic/require rewrites are near-zero.
- Block 9 done: reduced `collectOxcTemplateDependencies` hot-path overhead by reusing subtree reference analysis, caching binding resolution by reference position, and removing recursive `ancestors` array copies from the local walkers. Focused `collectOxcTemplateDependencies`, `applyOxcProcessors`, `prepareCode`, and `oxc-preeval-stage` suites are green.
- Block 10 done: re-measured against `recalc-20260418-v2-esm-evaluator-snap-r1` using snapshot `oxc-wip-20260424-r3`. Result: `Portal build 54.591s` (`-8.17%`), `Darkmatter build 15.676s` (`+1.67%`, stable), `Darkmatter total 17.476s` (`+1.60%`, stable). Versus `r2`, Darkmatter total improved by `0.541s` and typecheck by `0.198s`; the perf comparator reports overall `IMPROVEMENT`.
- Block 11 done: fused `collectOxcTemplateDependencies` whole-program setup so template / expression discovery, `bindings`, and `usedNames` come from one shared `Program` analysis that extraction reuses directly. The first full validation exposed only a TypeScript narrowing bug in the visitor; moving target collection into a pre-narrow helper kept the optimization and restored `build:types`. Focused `collectOxcTemplateDependencies`, `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, `@wyw-in-js/transform build:esm`, and `build:types` are green.
- Block 12 done: corrected perf measurement for the shared-analysis pass. The first `r4b` snapshot was invalid because `snapshot-release` measured committed `HEAD` after stashing local changes; the real dirty-head snapshot `oxc-wip-20260424-r5` was captured from a temporary tree commit. Result versus baseline: `Portal build 54.057s` (`-9.06%`), `Darkmatter build 15.260s` (`-1.02%`), `Darkmatter total 17.192s` (`-0.05%`), with only `Darkmatter typecheck 1.932s` (`+8.42%`) still in warning territory. Versus `r3`, Portal improved by `0.534s` and Darkmatter total by `0.284s`.
- Block 13 done: measured and rejected the first `removeDangerousCode` fixed-point rewrite. Focused tests and `build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r6` regressed versus `r5` enough to fail the experiment, so the candidate-list/cached-forbidden implementation was reverted instead of being compacted further.
- Block 14 done: reduced `processTemplate` lookup overhead inside `collectOxcTemplateDependencies` by replacing per-reference binding `filter(...).sort(...)[0]` with cached non-allocating selection and by precomputing insertion owners for extracted expressions instead of doing `program.body.find(...)` each time. Focused `collectOxcTemplateDependencies`-dependent suites and `build:types` were green, and dirty-head snapshot `oxc-wip-20260424-r7` compared to `r5` as overall `IMPROVEMENT`.
- Block 15 done: measured and rejected the recursive stack-cloning rewrite in `collectOxcTemplateDependencies`. Focused suites and `build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r8` regressed hard enough in Darkmatter to fail the experiment, so the stack-helper changes were backed out instead of being iterated further.
- Block 16 done: narrowed `processTemplate` reference traversal by caching scope-chain `hasLocalBinding` results inside `findReferences`. Focused `collectOxcTemplateDependencies`-dependent suites and `build:types` were green, and dirty-head snapshot `oxc-wip-20260424-r9` stayed stable versus `r7` while improving Darkmatter total/build and keeping overall status at `IMPROVEMENT` versus `r5`.
- Block 17 done: added internal `processTemplate:*` perf sublabels in `applyOxcProcessors` (`imports`, `usages`, `deps`, `reparse`, `usedNames`, `processors`, `cleanup`) and covered them through the existing Oxc preeval perf-label test. Focused `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` gates are green.
- Block 18 done: captured dirty-head snapshot `oxc-wip-20260424-r10` from a temporary tree commit and used the new `processTemplate:*` sublabels to isolate the remaining hot bucket. Result: `processTemplate:imports` is the dominant internal substep in both Darkmatter and Portal, far ahead of dependency extraction or processor instantiation.
- Block 19 done: added final processor-resolution caching in `processorLookup` and covered it with a focused `applyOxcProcessors` regression test. Focused `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` gates stayed green, but dirty-head snapshot `oxc-wip-20260424-r11` showed no material import-bucket win and regressed Darkmatter overall, so this closes the lookup-cache hypothesis rather than the hotspot.
- Block 20 done: switched `applyOxcProcessors` from the full `collectOxcExportsAndImportsFromProgram` path to an imports-only collector that preserves direct imports, transpiled CommonJS require imports, and namespace/member-derived processor tags while skipping export/reexport work. Focused `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, `collectExportsAndImports`, and `build:types` gates stayed green, and dirty-head snapshot `oxc-wip-20260424-r12` confirmed a real end-to-end win.
- Block 21 done: split `processTemplate:imports` into `imports:analysis` and `imports:lookup` perf sublabels and validated them through the focused `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, `collectExportsAndImports`, and `build:types` gates. Dirty-head snapshot `oxc-wip-20260424-r13` showed the residual imports cost was overwhelmingly lookup-driven, not AST-analysis-driven.
- Block 22 done: broadened the package-backed processor lookup cache so it reuses results across files instead of sharding by `filename` on a path where `definedTagsCache` was already global by package name. Focused correctness gates stayed green, and dirty-head snapshot `oxc-wip-20260424-r14` delivered the largest perf win so far.
- Block 23 done: measured and rejected a second `removeDangerousCode` slice after adding finer-grained sublabels and import-binding reuse. Dirty-head snapshot `oxc-wip-20260424-r15` improved Darkmatter build slightly (`13.371s`, `-1.52%` vs `r14`) but regressed Portal heavily (`51.629s`, `+9.53%`), so the experiment stays out despite focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, and `build:types` gates remaining green.
- Block 24 done: measured and rejected a `processorLookup` slice that combined shared package-fallback/file caches under `tagResolver` with lazy `resolvedSource` meta plus focused Oxc regressions for meta/fallback behavior. Dirty-head snapshot `oxc-wip-20260424-r16` stayed much better than the older `r5` baseline, but it regressed the current best `r14` state hard enough to reject (`Portal build 50.915s`, `+8.01%`; `Darkmatter build 13.612s`, `+0.26%`; `Darkmatter total 15.416s`, `+1.47%`).
- Block 25 done: added temporary lookup sublabels on the accepted path and profiled them with dirty-head snapshot `oxc-wip-20260424-r17`. The diagnostic slice is intentionally not a keeper (`Portal build 51.175s`, `+8.56%` vs `r14`; `Darkmatter total 15.766s`, `+3.77%`) because the instrumentation itself is too expensive, but it answered the right question: Darkmatter lookup is mostly package work (`258ms` total, `44ms` package), while Portal lookup is dominated by non-package miss overhead (`563ms` total, only `80ms` package, no meaningful cache-hit or tagResolver buckets showing up).
- Block 26 done: measured and rejected a miss-path allocation reduction in `processorLookup` that returned `null` instead of always materializing tuple/tag-source objects for unmatched imports. Focused `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, legacy processor-lookup coverage, and `build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r18` still lost badly to `r14` on Portal (`52.015s`, `+10.34%`) despite minor Darkmatter gains (`build 13.313s`, `-1.94%`; `total 15.050s`, `-0.94%`), so the slice stays reverted.
- Block 27 done: measured and rejected an `oxcPreevalTransforms` walker slice that replaced per-child `ancestors` array cloning with a shared mutable stack. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, and `build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r19` remained worse than `r14` across the board (`Portal build 49.346s`, `+4.68%`; `Darkmatter build 13.737s`, `+1.18%`; `Darkmatter total 15.505s`, `+2.05%`), so the traversal change stays out.
- Block 28 done: measured and rejected a narrower `removeDangerousCode` slice that reused `bindingKey`/binding-presence checks inside forbidden-reference detection and the main dangerous-code pass. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, and `build:types` stayed green, and Darkmatter’s local sublabel improved, but dirty-head snapshot `oxc-wip-20260424-r20` regressed the keeper state overall (`Portal build 48.695s`, `+3.30%`; `Darkmatter build 14.306s`, `+5.37%`; `Darkmatter total 16.270s`, `+7.09%`).
- Block 29 done: measured and rejected a request-level negative-cache slice in `shared/findPackageJSON` aimed at repeated processor-lookup misses for the same import source under different imported names. Focused `applyOxcProcessors` plus `shared`/`transform build:types` stayed green, and both Darkmatter and Portal plugin sublabels moved in the expected direction, but dirty-head snapshot `oxc-wip-20260424-r21` still lost to `r14` on aggregate (`Portal build 49.837s`, `+5.72%`; `Darkmatter build 13.656s`, `+0.58%`; `Darkmatter total 15.406s`, `+1.40%`), so the cache stays reverted.
- Block 30 done: measured and rejected a `collectOxcTemplateDependencies` slice that removed string-key dedupe in `findReferences` and skipped the replacement walk when `substituteConstants` had no constants to inline. Focused `collectOxcTemplateDependencies`, legacy `collectTemplateDependencies`, `applyOxcProcessors`, and `transform build:types` stayed green. The first clean dirty-head snapshot `oxc-wip-20260424-r22` looked promising (`Portal build 48.394s`, `+2.66%`; `Darkmatter build 13.273s`, `-2.24%`; `Darkmatter total 14.915s`, `-1.83%` vs `r14`), but a second clean rerun `oxc-wip-20260424-r24` still kept Portal slower (`48.865s`, `+3.66%`) while only modestly improving Darkmatter (`build 13.381s`, `-1.44%`; `total 14.984s`, `-1.38%`). An overlapping rerun `oxc-wip-20260424-r23` was invalid because a concurrent validate run corrupted the measurement path. Net: the block improves Darkmatter but not enough to beat the `r14` keeper overall, so it stays reverted.
- Block 31 done: added temporary `processTemplate:deps` sublabels around whole-program analysis, extraction, and constant substitution to test whether the remaining Portal-heavy cost still lived inside `collectOxcTemplateDependencies`. Focused `collectOxcTemplateDependencies`, `applyOxcProcessors`, `oxc-preeval-stage`, and `build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r25` proved the instrumentation itself is too expensive to keep (`Portal build 55.267s`; `Darkmatter build 16.296s`; `Darkmatter total 18.174s`). The useful signal was the aggregate bucket, not the rejected code: in the live preeval timings `processTemplate:deps` was only `99ms` in Darkmatter and `117ms` in Portal, far below `processTemplate:imports` (`318ms`/`964ms`) and `removeDangerousCode` (`472ms`/`1110ms`). Net: the `deps` path is no longer the main bottleneck, the diagnostic slice stays reverted, and future work should shift to the still-larger buckets.
- Block 32 done: measured and rejected an imports prefilter slice in `applyOxcProcessors` that added a dedicated AST walk to collect syntactic processor-callee candidates before running `getProcessorForImport` on imports. Focused `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` stayed green, and the dirty-head snapshot `oxc-wip-20260424-r26` did cut the Darkmatter import bucket (`processTemplate:imports 190ms`, `lookup 88ms`). But the extra whole-program pass did not pay for itself end to end: versus keeper `r14`, `Portal build` regressed to `50.798s` (`+7.76%`), `Darkmatter build` to `14.288s` (`+5.24%`), and `Darkmatter total` to `16.072s` (`+5.79%`). Net: the hypothesis was right about the import sub-bucket but wrong on aggregate, so the slice stays reverted.
- Block 33 done: measured and rejected a narrower `removeDangerousCode` slice that reused the already-parsed Oxc `Program` for import-binding collection instead of going back through the string-based import/export analyzer. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r27` still lost clearly to keeper `r14` (`Portal build 54.206s`, `+14.99%`; `Darkmatter build 13.735s`, `+1.16%`; `Darkmatter total 15.560s`, `+2.42%`). Net: the extra import-analysis pass was not the real bottleneck in `removeDangerousCode`, so the change stays reverted.
- Block 34 done: measured and rejected a zero-extra-pass `removeDangerousCode` slice that replaced repeated import-binding `find(...)` calls with a pre-indexed `Map` keyed by local name. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green, and Darkmatter remained close to the keeper (`build 14.142s`, `+4.16%`; `total 15.947s`, `+4.96%` vs `r14`). But dirty-head snapshot `oxc-wip-20260424-r28` still regressed Portal too much (`51.209s`, `+8.63%`), so the narrower import-matching optimization does not survive aggregate measurement and stays reverted.
- Block 35 done: measured and rejected a zero-extra-pass `removeDangerousCode` slice that removed hit-path ancestor allocations/reverse scans in removable-owner, promise-callback, function-replacement, and generated-helper detection. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green. Dirty-head snapshot `oxc-wip-20260424-r29` showed the intended local effect on Darkmatter (`type 1.670s`, `-7.48%`; `build 13.369s`, `-1.53%`; `total 15.039s`, `-1.01%` vs `r14`), but Portal still regressed too far (`51.795s`, `+9.88%`). Net: this helper-allocation slice improves the smaller workload but does not beat the current keeper on aggregate, so it stays reverted.
- Block 36 done: tested a Portal-oriented lookup slice that short-circuited non-package import sources (`./local`, aliases, virtual modules, `node:`-style specifiers) before package processor lookup and reused that miss path when `tagResolver` was absent. Focused `applyOxcProcessors`, `getTagProcessor*`, `oxc-preeval-stage`, `prepareCode`, and `build:types` stayed green. Dirty-head snapshot `oxc-wip-20260424-r30` produced the strongest internal lookup signal so far: Darkmatter `processTemplate:imports` dropped to `91ms` with `lookup 38ms`, and Portal `lookup` fell to `245ms` even though `analysis` rose to `503ms`. But the final aggregate timings still came out slower than the old keeper reference (`Portal build 52.705s`, `+11.81%`; `Darkmatter build 14.152s`, `+4.23%`; `Darkmatter total 15.966s`, `+5.09%` vs `r14`). Because this directly conflicts with the much better internal sublabels and multiple recent slices have all clustered around a slower Portal envelope than the original `r14`, this candidate should stay reverted until a fresh contemporary keeper baseline is captured.
- Block 37 done: investigated the failed keeper recalibration snapshot instead of treating `r31` as a valid baseline. The current built `@wyw-in-js/transform` succeeds on a direct `FormFieldStyles.ts` transform, on a synthetic shared-cache repro that widens exports from the same child module, and on a batch transform of all `111` real Darkmatter files importing `@/styles/textStyles.ts` with one shared cache/broker. Net: the `r31` `textStyles.breakWord` failure is not reproducible on the reverted keeper tree and should be treated as an invalid/transient packaged-snapshot result until a fresh snapshot proves otherwise.
- Block 38 done: captured a fresh packaged keeper baseline from the reverted tree as `oxc-keeper-20260424-r32`, and the packaged Darkmatter path passed end to end. The valid contemporary baseline is now `Portal build 52.148s`, `Darkmatter typecheck 1.944s`, `Darkmatter build 15.281s`, `Darkmatter total 17.225s`. Historical dirty-head snapshots like `r14` remain useful as secondary reference points, but they are no longer the primary gate for new work because `r31` proved the old comparison path was contaminated by invalid snapshot state.
- Block 39 done: revived the Block 36 processor-lookup miss fast-path on top of the current keeper and added direct `processorLookup` coverage for non-package misses, `tagResolver` resolution, `tagResolver` misses, and package-backed lookups. Focused `processorLookup`, `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` stayed green. Packaged snapshot `oxc-wip-20260424-r33` is an overall `IMPROVEMENT` versus keeper `r32`: `Portal build 49.064s` (`-5.91%`), `Darkmatter typecheck 1.838s` (`-5.45%`), `Darkmatter build 15.874s` (`+3.88%`, stable), `Darkmatter total 17.712s` (`+2.83%`, stable). The validate harness still prints a stale “expected active @wyw-in-js dependencies” tag check during restore, but the run exits `0` and the snapshot is valid.
- Block 40 done: revived the zero-extra-pass `removeDangerousCode` helper-allocation slice on top of keeper `r33` by removing repeated reversed-ancestor copies/lookups in removable-owner, promise-callback, function-replacement, and generated-helper detection. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `processorLookup`, and `build:types` stayed green. Snapshot `oxc-wip-20260424-r34` is an overall `IMPROVEMENT` versus keeper `r33`: `Portal build 49.042s` (`-0.04%`, stable), `Darkmatter typecheck 1.714s` (`-6.75%`), `Darkmatter build 13.111s` (`-17.41%`), `Darkmatter total 14.825s` (`-16.30%`). The validate harness still burns time in a stale portal graph expectation/retry after timings are already written, but the snapshot timings themselves are valid and clearly better than `r33`.
- Block 41 done: measured and rejected a linear-time chunk-assembly fast path for `applyOxcProcessors` replacements, keeping the old descending splice algorithm as fallback for overlaps. Focused `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` stayed green, but snapshot `oxc-wip-20260424-r35` regressed both workloads against keeper `r34`: `Portal build 51.703s` (`+5.43%`), `Darkmatter build 14.367s` (`+9.58%`), `Darkmatter total 16.140s` (`+8.87%`). The candidate is reverted.
- Block 42 done: re-tested the overlap-safe replacement fast path only inside `collectOxcTemplateDependencies` on top of keeper `r34`. Focused `extractExpression`, `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` stayed green, but packaged snapshot `oxc-wip-20260424-r36` was only `STABLE` and not a keeper: `Portal build 51.111s` (`+4.22%`), `Darkmatter build 13.271s` (`+1.22%`), `Darkmatter total 14.962s` (`+0.92%`) versus `r34`. The candidate is reverted.
- Block 43 done: measured and rejected a `collectOxcTemplateDependencies` constant-substitution slice that reused the existing `findReferences(...)` results instead of doing a second identifier-rewrite walk. Focused `extractExpression`, `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` stayed green, and the internal Darkmatter `processTemplate:deps` bucket dropped to `93ms`, but packaged snapshot `oxc-wip-20260424-r37` regressed badly on aggregate versus keeper `r34`: `Portal build 55.655s` (`+13.48%`), `Darkmatter typecheck 1.804s` (`+5.25%`), `Darkmatter build 14.377s` (`+9.66%`), `Darkmatter total 16.181s` (`+9.15%`). The candidate is reverted.
- Block 44 done: measured and rejected a mutable ancestor-stack rewrite for the shared `visit(...)` helper in `oxcPreevalTransforms`. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green, and the internal target bucket did improve (`removeDangerousCode 454ms -> 417ms` in Darkmatter, `1208ms -> 978ms` in Portal), but packaged snapshot `oxc-wip-20260424-r38` still regressed against keeper `r34`: `Portal build 49.703s` (`+1.35%`, stable), `Darkmatter typecheck 1.800s` (`+5.02%`), `Darkmatter build 13.909s` (`+6.09%`), `Darkmatter total 15.709s` (`+5.96%`). The candidate is reverted.
- Block 45 done: accepted a narrow `processorLookup` fast path that returns immediately for safe non-package misses when `tagResolver` is absent, skipping cache-key creation and Map churn on the Portal-heavy miss path while keeping package-backed lookups and `tagResolver` behavior unchanged. Focused `processorLookup`, `applyOxcProcessors`, `prepareCode`, `oxc-preeval-stage`, and `build:types` stayed green. Packaged snapshot `oxc-wip-20260424-r39` is `STABLE` versus keeper `r34` and acceptable as a keeper: `Portal build 48.855s` (`-0.38%`), `Darkmatter typecheck 1.729s` (`+0.88%`), `Darkmatter build 13.462s` (`+2.68%`), `Darkmatter total 15.191s` (`+2.47%`). Internal Portal lookup also moved in the right direction (`imports:lookup 208ms -> 176ms`).
- Block 46 done: measured and rejected a micro-optimization that replaced callback-based ancestor scans in `isTypeContext` / `isInsideTypeof` with manual reverse loops inside `oxcPreevalTransforms`. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green, and Darkmatter improved locally (`type 1.672s`, `build 13.256s`, `total 14.928s`), but packaged snapshot `oxc-wip-20260424-r40` regressed Portal too much versus keeper `r39`: `Portal build 52.033s` (`+6.50%`). The candidate is reverted.
- Block 47 done: added temporary internal sublabels around `applyOxcProcessors` cleanup and processor creation to open up the remaining hidden `processTemplate` cost, then measured them with dirty-head snapshot `oxc-wip-20260424-r41`. The diagnostic slice is intentionally rejected because the instrumentation itself regressed aggregate timings hard (`Portal build 53.683s`, `+9.88%`; `Darkmatter total 15.899s`, `+4.66%` vs keeper `r39`), but it answered the attribution question: in Darkmatter the residual `processTemplate` cost sat overwhelmingly in `processors:create` (`318ms` out of `405ms`), while `params`/`callback` were negligible and cleanup was not the main live target. The extra telemetry stays reverted.
- Block 48 done: measured and rejected a narrow `applyOxcProcessors` slice that tried to replace repeated `isTagReferenced(...)` program scans with one lazy file-wide `collectReferencedNames(program)` set and also reused `getTagOwner(...)` for display-name lookup. Focused `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green, and the internal Darkmatter buckets moved in the expected direction (`processTemplate 1012ms -> 874ms`, `processors 405ms -> 271ms`; Portal `processors 96ms -> 70ms`). But packaged snapshot `oxc-wip-20260424-r42` still regressed every aggregate metric versus keeper `r39` (`Portal build 52.065s`, `+6.57%`; `Darkmatter typecheck 1.933s`, `+11.80%`; `Darkmatter build 14.518s`, `+7.84%`; `Darkmatter total 16.451s`, `+8.29%`). The candidate is reverted and should not be revived without a fresh explanation for the aggregate mismatch.
- Block 49 done: measured and rejected a `createProcessor` micro-slice that made `createOxcAstService` lazy-initialize its `usedNames` allocator so processors that never request helper imports would avoid cloning the name set. Focused `oxc-ast-service`, `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, and `build:types` stayed green, but packaged snapshot `oxc-wip-20260424-r43` regressed every aggregate metric versus keeper `r39`: `Portal build 51.897s` (`+6.23%`), `Darkmatter typecheck 1.856s` (`+7.35%`), `Darkmatter build 15.084s` (`+12.05%`), `Darkmatter total 16.940s` (`+11.51%`). Internal sublabels also failed to support the hypothesis (`Darkmatter processTemplate:processors 271ms -> 425ms`, Portal `70ms -> 86ms`). The candidate is reverted.
- Block 50 done: measured and rejected a `getClassNameAndSlug` file-path cache that reused per-file slug vars and hoisted the generated-meta logger extension. Focused `oxc-ast-service`, `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `@wyw-in-js/processor-utils build:types`, and `@wyw-in-js/transform build:types` stayed green, but dirty-head snapshot `oxc-wip-20260424-r44` regressed the aggregate versus keeper `r39`: `Portal build 51.987s` (`+6.41%`), `Darkmatter typecheck 1.792s` (`+3.64%`), `Darkmatter build 14.099s` (`+4.73%`), `Darkmatter total 15.891s` (`+4.61%`). The candidate is reverted.
- Block 51 done: invalidated packaged snapshots `oxc-wip-20260424-r45` and `r46` instead of treating them as perf data. Both full `validate.sh --snapshot-release` runs died before `Captured snapshot` in the Portal phase on an external environment failure: `@portal/ui` could not resolve the root `date-fns` entry because the local `date-fns@4.1.0` package under Portal exposed `module`/exports to `index.js` while the installed package directory only contained `index.cjs`. Net: full packaged perf measurement is temporarily blocked outside `wyw-in-js`, so further work should not spend more time on packaged snapshots until that Portal dependency state is repaired.
- Block 52 done: measured and rejected the `createOxcAstService` shared-prototype/class rewrite with a Darkmatter-only A/B loop while packaged snapshots were blocked. Focused `oxc-ast-service`, `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `build:esm`, and `build:types` stayed green. With local `@wyw-in-js/*` symlinked into Darkmatter and installed `@linaria/*` left untouched, the candidate was worse than the reverted keeper on the same workload: `pnpm type` stayed flat (`1.29s` -> `1.28s`), but `pnpm exec vite build` regressed (`real 11.55s` -> `11.07s` baseline; WyW internal total `6631ms` -> `6342ms`). Internal attribution did not rescue the hypothesis: `processTemplate:processors` fell (`359ms` vs `397ms`), but `imports`, `removeDangerousCode`, and overall transform total all moved the wrong way. The prototype/class rewrite stays reverted.
- Block 53 done: accepted a zero-risk `import.meta.env` fast-path in `replaceImportMetaEnvWithOxc` that returns early when the source does not contain an `import.meta.env` pattern, avoiding a full parse/walk on files that cannot match. Focused `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `build:esm`, and `build:types` stayed green. On the Darkmatter-only loop, `pnpm type` stayed flat (`1.28s` -> `1.29s`), while `pnpm exec vite build` improved (`real 11.07s` -> `10.92s`, `-1.35%`) and WyW internal total dropped (`6342ms` -> `6248ms`, `-1.48%`). The targeted bucket collapsed exactly as intended (`importMetaEnv 159ms` -> `6ms`), so this slice is safe to keep even before packaged Portal snapshots are available again.
- Block 54 done: accepted a matching CommonJS-import fast-path in `collectImportBindings` so `removeDangerousCode` no longer falls back to `collectOxcExportsAndImports(...)` on files without any `require(...)` pattern. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `build:esm`, and `build:types` stayed green. On top of the Block 53 keeper, the Darkmatter-only loop improved again: `pnpm type` `1.29s` -> `1.21s`, `pnpm exec vite build` `real 10.92s` -> `10.59s` (`-3.02%`), and WyW internal total `6248ms` -> `5986ms` (`-4.19%`). The live attribution also confirms the mechanism: `removeDangerousCode` dropped from `673ms` to `471ms` while the new guard left the already-fixed `importMetaEnv` bucket near zero (`8ms`).
- Block 55 done: kept a stable `window`-token fast-path in `removeDangerousCodeWithOxc` so `collectWindowScopedNames(program)` does not run on files that cannot contain `window.foo`-derived globals. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `build:esm`, and `build:types` stayed green. On top of the Block 54 keeper, the Darkmatter-only loop stayed effectively flat on aggregate (`pnpm type` `1.21s` -> `1.22s`, `pnpm exec vite build` `real 10.59s` -> `10.61s`, internal total `5986ms` -> `5994ms`), but the targeted bucket still improved (`removeDangerousCode 471ms` -> `415ms`). This is acceptable as a low-risk stable layer even though it is no longer a large aggregate win.
- Block 56 done: measured and rejected a pure owner-lookup micro-slice that replaced `getTagOwner` / `getDisplayName` reverse-copy scans with a shared reverse loop. Focused `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `build:esm`, and `build:types` stayed green, but the Darkmatter-only loop did not hold the aggregate: `pnpm type` regressed (`1.22s` -> `1.27s`) and wall-clock build also moved the wrong way (`real 10.61s` -> `10.72s`) even though the internal total was roughly flat (`5994ms` -> `5965ms`). The change stays reverted; further work should avoid ancestor-allocation micro-optimizations unless there is a stronger measured reason.
- Block 57 done: accepted a `processTemplate` de-duplication slice that resolves each usage’s processor match once in `collectProcessorUsages` and carries the resolved processor plus `collapseQualifiedCallee` flag into the processors loop, instead of recomputing the same callee analysis later. Focused `applyOxcProcessors`, `oxc-preeval-stage`, `prepareCode`, `oxc-prepare-code`, `build:esm`, and `build:types` stayed green. On top of the Block 55 keeper, the Darkmatter-only loop improved cleanly: `pnpm type` `1.22s` -> `1.21s`, `pnpm exec vite build` `real 10.61s` -> `10.46s` (`-1.41%`), and WyW internal total `5994ms` -> `5860ms` (`-2.24%`). The live buckets also moved in the right direction overall: `processTemplate 901ms` -> `873ms`, `preeval 1425ms` -> `1375ms`, `evaluator 417ms` -> `395ms`, while `processors` itself stayed flat (`316ms` -> `313ms`), which suggests the win came from removing repeated callee-shape work around, not inside, processor instantiation.
- Block 58 done: accepted the first dedicated shaker/evaluator reuse slice by switching `oxcShaker` from “parse once, then call `collectOxcExportsAndImports(code, filename)` which parses again” to the existing `collectOxcExportsAndImportsFromProgram(...)` path over the already-parsed `Program`. Focused `prepareCode`, `oxc-prepare-code`, `oxc-preeval-stage`, `build:esm`, and `build:types` stayed green. On top of the Block 57 keeper, the Darkmatter-only loop improved again: `pnpm type` `1.21s` -> `1.20s`, `pnpm exec vite build` `real 10.46s` -> `10.40s` (`-0.57%`), and WyW internal total `5860ms` -> `5776ms` (`-1.43%`). The targeted bucket moved exactly as expected: `transform:evaluator 395ms` -> `280ms`, confirming that the shaker still had real duplicated parse work left even after the preeval wins.
- Block 59 done: accepted the second shaker/evaluator reuse slice by making `removeUnusedImportSpecifiers(...)` return the validated parsed result for `nextCode`, then feeding that same parsed result into the final `collectOxcExportsAndImportsFromProgram(...)` call instead of parsing the identical text one more time. Focused `prepareCode`, `oxc-prepare-code`, `oxc-preeval-stage`, `build:esm`, and `build:types` stayed green. On top of the Block 58 keeper, the Darkmatter-only loop improved again: `pnpm type` drifted slightly slower (`1.20s` -> `1.24s`), but the main build still improved (`real 10.40s` -> `10.36s`, `-0.38%`) and WyW internal total dropped (`5776ms` -> `5749ms`, `-0.47%`). The live buckets stayed directionally consistent with the hypothesis: `transform:evaluator` held in the low `280ms` range while `processTemplate` and `removeDangerousCode` also remained below the older keeper.
- Block 60 done: fixed a real downstream cache regression before resuming perf work. Added `wyw`-side regression coverage for partial evaluated-cache reuse and confirmed the original `linaria` cache contract on a live linked workspace. The fix has two parts: during prepare/eval we no longer force `__wywPreval` onto cached evaluated dependencies unless the dependency actually exports it, and concurrent root workflows now wait for in-flight processing instead of racing ahead with incomplete metadata. Focused `processImports`, new transform cache tests, `issue-96` concurrency coverage, `build:esm`, and `build:types` are green; `@linaria/testkit` targeted cache tests are green again after rebuilding `packages/transform/esm`.
- Block 61 done: restored end-to-end `validate` after the cache fix. The attempted `@wyw-in-js/vite` CJS compatibility surface was rejected as unnecessary and reverted. The actual downstream fix was narrower: `portal/app/vite.config.ts` now loads the ESM-only `@wyw-in-js/vite` entry through `await import(...)`, which matches the current package surface and removes the config-time `require()` failure. `portal/app` build is green, full `./validate.sh` is green again, and the current checkpoint is once more suitable for perf measurement.
- Block 62 done: captured a fresh 3-run perf sample from the repaired local tree using snapshot refs that include the current WyW working tree plus the live Portal config change. Candidate snapshots: `probe-20260425-183745`, `head-20260425-183745-r2`, `head-20260425-183745-r3`. Candidate means: Portal `48.769s` (`σ=0.577s`), Darkmatter typecheck `1.706s` (`σ=0.056s`), Darkmatter build `13.267s` (`σ=0.294s`), Darkmatter total `14.973s` (`σ=0.319s`). Against `recalc-20260418-v2-esm-evaluator-snap-r1`, this is a clear improvement (`Portal -17.96%`, `Darkmatter build -13.95%`, `Darkmatter total -12.95%`). Against the last accepted Oxc keeper `oxc-wip-20260424-r39`, the current state stays inside stable noise with a slight mean improvement on every reported metric (`Portal -0.18%`, `Darkmatter build -1.45%`, `Darkmatter total -1.44%`).
- Block 63 done: finished the `@wyw-in-js/babel-preset` compat follow-up without changing its runtime role. The preset still delegates to the Oxc-backed transform path, but now emits a once-per-process deprecation warning, warns when `.mjs` / `.mts` WyW config files are loaded through the synchronous wrapper, and surfaces clearer guidance when inline preset options are not serializable. Focused `packages/babel-preset` tests are green, `build:esm` is green, and the branch-level release note/docs now describe the preset as a deprecated compatibility wrapper rather than a primary integration path.
- Block 64 done: bumped `oxc-parser` / `oxc-transform` from `0.86.0` to `0.127.0` and repaired the sync-call surface without changing the Oxc-first architecture. The main compatibility break was upstream API shape, not semantics: `transform(...)` is now async, while the sync path moved to `transformSync(...)`. The fix was to move our build/esbuild callers to the public sync API and to update `oxcEmit`'s custom loader to prefer `transformSync` when the module namespace exposes both. Focused `packages/transform` Oxc suites, `packages/esbuild` adapter suites, `build:esm`, and `build:types` are green again. The remaining work in this block is end-to-end validation and a fresh perf comparison against the pre-upgrade Oxc baseline.
- Current block: run full validation on the `0.127.0` upgrade, then capture a fresh multi-run perf sample and compare it to the last pre-upgrade green sample to see whether the newer Oxc release changed runtime cost in either direction.
- Block 65 done: added a local package-level benchmark harness in `packages/transform/scripts/perf-complex-scenarios.ts` plus `perf:complex-scenarios`. It generates synthetic mini-projects and runs a shared-cache multi-file transform sweep with phase timing breakdowns, instead of relying on flaky timing assertions in unit tests. Covered scenario shapes: deep wildcard `export *` barrels, many files importing many constants from one shared module directly, the same shared-constants fanout with helper-function application before CSS extraction, and overlapping shared-constant subsets with a second named-reexport layer. The runner reports wall-clock plus internal `transform:preeval`, `processTemplate`, `imports`, `lookup`, `processors`, `removeDangerousCode`, and `evaluator` buckets, with `--warmup 1` by default to avoid cold-start distortion when benchmarking a single scenario in isolation.
- Initial smoke findings from the new harness: on `small`/`medium`, `wildcard-barrel-fanout` is clearly the hottest synthetic case and concentrates cost in `imports` / `lookup`, while the functional shared-constants fanout consistently lifts `processTemplate` and `evaluator` above the direct-constant variant without needing barrel noise. This gives us a fast local perf probe for targeted changes before going back to repo-wide Portal/Darkmatter validation.
- Block 66 done: fixed a real `removeDangerousCodeWithOxc` scope bug introduced by the Oxc walker model. The remover used incremental binding discovery, so a same-module `FunctionDeclaration` named like a forbidden global (`fetch`) was treated as global until the traversal physically reached that declaration, and function declarations were only being registered in their own function scope instead of the parent lexical scope. Added scope-level name predeclaration for the active lexical scope before the dangerous-code pass runs, then pinned the behavior with a new regression test that preserves both pre- and post-declaration calls to a local `fetch`. Also added an explicit regression that a removed top-level `await fetch(...)` call must not leave syntactically broken output. Focused `oxc-preeval-transforms`, `oxc-preeval-stage`, `preeval`, `build:esm`, and `build:types` are green.
