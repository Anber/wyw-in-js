{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["tsconfig.json", "scripts/build-esm-oxc.mjs"],
  "extends": ["//"],
  "pipeline": {
    "build:esm": {
      "dependsOn": ["^build:esm"],
      "outputs": ["esm/**"]
    },
    "build:types": {
      "dependsOn": ["^build:types", "@wyw-in-js/ts-config#check"],
      "outputs": ["types/**"]
    },
    "lint": {
      "dependsOn": ["@wyw-in-js/eslint-config#check"]
    },
    "test": {
      "dependsOn": []
    }
  }
}
